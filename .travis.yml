# References:
# - http://about.travis-ci.org/docs/user/languages/python/
# - http://docs.travis-ci.com/user/docker/
language: python
cache: pip

sudo: required
services:
  - docker

before_install:
  - docker build -t ssato/fleure -f pkg/Dockerfile .
  - docker run ssato/fleure /bin/sh -c "cd /tmp/fleure && bash -x ./pkg/runtest.sh"
  - docker ps -a
  - docker info

script:
  - "./pkg/runtest.sh"

notifications:
  email:
    #on_success: always # default: change
    on_failure: always

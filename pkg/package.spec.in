# Ref. https://fedoraproject.org/wiki/Packaging:Python
%global srcname fleure
%global sumtxt  Package level static analysis tool for RPM-based Linux systems
%global cli     %{srcname}_cli

Name:           python-%{srcname}
Version:        @VERSION@
Release:        1%{?dist}
Summary:        %{sumtxt}
Group:          Applications/System
License:        AGPLv3+ and GPLv3+
URL:            https://github.com/ssato/%{srcname}
Source0:        %{srcname}-%{version}.tar.gz
BuildArch:      noarch
BuildRequires:  python2-devel python3-devel
BuildRequires:  python-setuptools python3-setuptools
# for pygettext.py:
BuildRequires:  python2-tools python3-tools

%description
A package level static analysis tool for systems running RPM-based linux
distributions such like RHEL (primary target) and Fedora.

%package     -n python2-%{srcname}
Summary:        %{sumtxt}
Requires:       python-%{srcname}-common
Requires:       python-beautifulsoup4
# Available from: https://copr.fedoraproject.org/coprs/ssato/python-anyconfig/
Requires:       python-anyconfig
Requires:       python-anytemplate
Requires:       python-bunch
Requires:       python-networkx
Requires:       python-nltk
Requires:       python-sqlalchemy
Requires:       python-tablib
Requires:       graphviz
Requires:       libosinfo
Requires:       rpm-python
%{?python_provide:%python_provide python2-%{srcname}}

%description -n python2-%{srcname}
A package level static analysis tool for systems running RPM-based linux
distributions such like RHEL (primary target) and Fedora.

%package     -n python3-%{srcname}
Summary:        %{sumtxt}
Requires:       python-%{srcname}-common
Requires:       python3-beautifulsoup4
Requires:       python3-anyconfig
Requires:       python3-anytemplate
Requires:       python3-bunch
Requires:       python3-networkx
Requires:       python3-nltk
Requires:       python3-sqlalchemy
Requires:       python3-tablib
Requires:       rpm-python3
%{?python_provide:%python_provide python3-%{srcname}}

%description -n python3-%{srcname}
A package level static analysis tool for systems running RPM-based linux
distributions such like RHEL (primary target) and Fedora.

%package     -n python-%{srcname}-common
Summary:        %{srcname} common data files

%description -n python-%{srcname}-common
Common data files of %{srcname}.

%prep
%setup -q -n %{srcname}-%{version}

%build
%py3_build
%py2_build

%install
%py3_install
mv $RPM_BUILD_ROOT%{_bindir}/%{srcname}_cli $RPM_BUILD_ROOT%{_bindir}/%{cli}-3
%py2_install
mv $RPM_BUILD_ROOT%{_bindir}/%{srcname}_cli $RPM_BUILD_ROOT%{_bindir}/%{cli}-2
# Default: python-2 version
(cd $RPM_BUILD_ROOT%{_bindir}/ && ln -s %{cli}-2 %{cli})

%files -n python2-%{srcname}
%doc README.rst
%{python2_sitelib}/*
%{_bindir}/%{cli}-2
%{_bindir}/%{cli}

%files -n python3-%{srcname}
%doc README.rst
%{python3_sitelib}/*
%{_bindir}/%{cli}-3

%files -n python-%{srcname}-common
%{_sysconfdir}/%{srcname}.d/*.yml
%{_datadir}/%{srcname}/templates/2/*/*.*

%changelog
* Sun Oct  4 2015 Satoru SATOH <ssato@redhat.com> - 0.1-1
- Initial packaging originally came from the part of rpmkit
